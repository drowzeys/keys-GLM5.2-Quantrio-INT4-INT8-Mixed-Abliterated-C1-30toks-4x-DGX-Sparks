# Standing serve env — SPEED=1 latency profile (C1 ≈ 30 tok/s structured)
# Prefer recipe/cluster.env (includes NODES + fabric). This file is serve-knobs only.

IMAGE=vllm-node-tf5-glm52-b12x:probe-modded
NAME=vllm_qt200k
PORT=8210
UTIL=0.86
KV_BYTES=7008000000
MAX_MODEL_LEN=128000
MTP_K=4
SPEED=1
# SPEED=1 → max_num_seqs=1

# Full cluster config:
#   cp recipe/cluster.env.example recipe/cluster.env
#   bash scripts/oneshot.sh --all
#   # or: bash serve/launch-keyspark.sh
